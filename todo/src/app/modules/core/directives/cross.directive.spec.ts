import { CrossDirective } from './cross.directive';

describe('CrossDirective', () => {
  it('should create an instance', () => {
    const directive = new CrossDirective();
    expect(directive).toBeTruthy();
  });
});
