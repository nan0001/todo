import { CollapseColumnsDirective } from './collapse-columns.directive';

describe('CollapseColumnsDirective', () => {
  it('should create an instance', () => {
    const directive = new CollapseColumnsDirective();
    expect(directive).toBeTruthy();
  });
});
